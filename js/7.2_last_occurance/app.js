const paragraph = "The more you know, the more you know that you dont know";

const searchTerm = "you";

console.log(
  `The index of the first "${searchTerm}" from the end is ${paragraph.lastIndexOf(
    searchTerm
  )}`
);
